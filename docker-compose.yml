---
services:
  s-ui:
    image: alireza7/s-ui
    container_name: s-ui
    volumes:
      - "./db:/app/db"
      - "./cert:/app/cert"
    restart: unless-stopped
    network_mode: "host"
    entrypoint: "./entrypoint.sh"
